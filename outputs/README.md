# Outputs and listening samples

Generated evaluation and inference outputs belong here. A stable listening set,
when one exists, should use 3--5 fixed sample IDs under `outputs/examples/` and
record the degraded, reference, and enhanced paths together with the checkpoint
and configuration that produced them.

This review snapshot contains no verified Hybrid-UniSE enhanced audio. The
audio under `assets/upstream/` is inherited UniSE demo material, not output from
this project.
