# Pretrained assets

This directory is reserved for externally downloaded model weights, such as
UniSE, BiCodec, WavLM, or X-Codec assets. They are not checkpoints trained by
this project.

No pretrained weights are included in the review snapshot. Runtime weight
files are ignored by Git.
