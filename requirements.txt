# Minimal code-inspection and synthetic-smoke environment.
# Choose the PyTorch build appropriate for your platform and accelerator.
numpy>=1.26,<3
pytorch-lightning>=2.4,<3
PyYAML>=6,<7
soundfile>=0.12,<1
torch>=2.5,<3
transformers>=4.46,<5

# Optional quality-loss adapters are not installed by default:
# asteroid>=0.7,<1
# torchaudio>=2.5,<3
